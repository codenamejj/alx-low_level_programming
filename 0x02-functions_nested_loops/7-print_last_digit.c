#include "main.h"
/**
  * print_last_digit - print last num
  * @n: num being checked
  *
  * Return: 0 always
  */
int print_last_digit(int n)
{
	int r;

	if (r == n % 10)
	{
		return (r);
	}

	_putchar(r);

	return (0);

}
