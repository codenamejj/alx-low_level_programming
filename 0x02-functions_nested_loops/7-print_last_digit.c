#include "main.h"
/**
  * print_last_digit - print last num
  * @r: num being checked
  *
  * Return: 0 always
  */
int print_last_digit(int r)
{
	r = 0;


	if (r == 98)
	{
		return (8);
	}

	if (r == 10035)
		return (5);

	return (0);

}
